spring:
  thymeleaf:
    mode: HTML
  datasource:
    driver-calss-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/blogs?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    #show-sql: true

#配置评论的统一头像
comment.avatar: /images/avatar.png